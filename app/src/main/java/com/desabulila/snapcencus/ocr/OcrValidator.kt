package com.desabulila.snapcencus.ocr

/* class OcrValidator {

    private fun isNumberExist(data: String): Boolean {
        return data.any { it.isDigit() }
    }

    fun isValidJenisKelamin(data: String): Boolean {
        val normalizeData = data.lowercase().replace(":", "")
        return normalizeData.con
    }
} */